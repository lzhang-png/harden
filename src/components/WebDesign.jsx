import './WebDesign.css';

export default function WebDesign() {
  return (
    <div className="page-container web-design-page">
      <header className="page-header web-design-header">
        <h1 className="page-title">Web design</h1>
        <p>
          Use this space for site structure, wireframes, or live comps. Replace this copy with
          your web-specific deliverables when ready.
        </p>
      </header>
      <section className="web-design-placeholder" aria-hidden="true" />
    </div>
  );
}
